//ts编译器配置文件
//**表示任意目录
//*表示任意文件
//include ./**/* node终端根目录下任意文件任意文件tsc后都会被编译
//exclude 不包含
//extends 继承配置文件
//files 列出需要编译的指定文件

//compilerOptions编译选项
//target编译后的es版本
//moudle模块化es版本(export import)
//lib指定项目中使用的库，一般不改
//outDir改变编译后输出文件位置
//outFile编译后所有文件合并成一个,请勿和模块化一起用，或system
//allowJs是否对js文件进行编译，默认false
//checkJs检查js是否符合语法规范，默认false
//removeComments是否移除注释，默认false
//noEmit编译不生成编译文件js，默认false
//noEmitOnError编译报错不生成编译文件，默认false
//alwaysStrict编译后文件严格模式是否开启，默认false
//noImplicitAny不允许隐式any，默认false是允许
//noImplicitThis不允许不明确类型的this，默认false
//strictNullChecks严格检查空值，默认false（如果DOM获取没获取到）
//strict以上严格总开关，默认false
{
    "include":[
        "obj.ts"
    ],
    "exclude":[
        "or.ts"
    ],
    "compilerOptions": {
        "strict": false,
        "target": "ES6",
        "module": "ES6",
        "outDir": "./outDir",
        "allowJs": false,
        "checkJs": false,
        "removeComments": false,
        "noEmit": false,
        "noEmitOnError": false,
        "alwaysStrict": false,
        "noImplicitAny": false,
        "noImplicitThis": false,
        "strictNullChecks": false,
    }
}
